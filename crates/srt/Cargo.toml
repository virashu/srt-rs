[package]
name = "srt"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
tracing = "0.1.41"

tokio = { version = "1.50.0", features = ["net", "rt", "sync"], optional = true }

[features]
default = ["tokio"]
tokio = ["dep:tokio"]

[lints]
workspace = true
